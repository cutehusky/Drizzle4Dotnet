namespace Drizzle4Dotnet.Oracle.Operators;

/// <summary>
/// Oracle-specific SQL functions.
/// Organized into subclasses by category:
/// - DateTime: SYSDATE, SYSTIMESTAMP, ADD_MONTHS(), TRUNC(), etc.
/// - String: INSTR(), SUBSTR(), LENGTH(), CONCAT(), DECODE(), NVL(), etc.
/// - Numeric: ROUND(), TRUNC(), MOD(), POWER(), etc.
/// - Json: JSON_VALUE(), JSON_QUERY(), JSON_TABLE(), etc.
/// - Analytic: ROW_NUMBER(), RANK(), LAG(), LEAD(), etc.
/// - Info: USER, UID, ROWID, ROWNUM, etc.
/// </summary>
public static partial class OracleFunctions
{
}
