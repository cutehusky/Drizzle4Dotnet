namespace Drizzle4Dotnet.Sqlite.Operators;

/// <summary>
/// SQLite-specific SQL functions.
/// Organized into subclasses by category:
/// - DateTime: datetime(), date(), time(), strftime(), julianday()
/// - String: instr(), substr(), replace(), trim(), etc.
/// - Numeric: random(), abs(), round(), etc.
/// - Json: json_extract(), json_object(), json_array(), etc.
/// - Info: last_insert_rowid(), sqlite_version(), changes(), total_changes()
/// </summary>
public static partial class SqliteFunctions
{
}
